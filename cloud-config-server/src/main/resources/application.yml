eureka:
  client:
    service-url:
       default-zone: http://localhost:8761/eureka/
  instance:
    hostname: localhost
   
server:  port: 8084
 
spring:  application:    name: CONFIG-SERVER
  cloud:    config:      server:        git:          uri: https://github.com/vempallireddybasha/CONFIG-SERVER
          clone-on-start: true

